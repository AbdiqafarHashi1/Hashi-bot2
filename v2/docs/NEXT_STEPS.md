# NEXT_STEPS

1. Build HashiBridge + MT5 adapter and Agent EA architecture using the same adapter contract and certification harness.
2. Add persistent backend credential vault and encrypted account-scoped client factories.
3. Build shared Protection Engine over adapter position/order/protection snapshots.
4. Add production exchange SDK clients behind the existing mock-certified contract.
